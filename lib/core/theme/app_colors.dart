import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFFFFD600);
  static const Color primaryDark = Color(0xFFE5C000);

  // Background
  static const Color backgroundLight = Color(0xFFF4F2FF);
  static const Color backgroundDark = Color(0xFF221F10);

  // Surface
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textMain = Color(0xFF2D2345);
  static const Color textMuted = Color(0xFF9CA3AF);

  // Accent
  static const Color accent = Color(0xFFFF4785);

  // Semantic
  static const Color success = Color(0xFF00C896);
  static const Color info = Color(0xFF4C6FFF);

  // Misc.
  static const Color divider = Color(0xFFE5E7EB);
  static const Color cardBorder = Color(0xFFF9FAFB);
}
